mutation CreateGeofenceEvent($input: CreateGeofenceEventInput!) {
    createGeofenceEvent(input: $input) {
        geofenceEvent {
            id
            timestamp
            eventType
            userLocation {
                latitude
                longitude
            }
            userId
        }
    }
}